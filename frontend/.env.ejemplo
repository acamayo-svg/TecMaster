# Variable para el proyecto Frontend en Vercel (Settings → Environment Variables).
# Configura VITE_API_URL en Vercel con la URL del backend (sin barra final).

VITE_API_URL=https://tu-backend.vercel.app
